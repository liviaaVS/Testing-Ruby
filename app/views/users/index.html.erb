
<h1 class="pink-200 mt-4">Listagem de Usuários</h1>

<div class="card p-3 w-50">
  <ul class="list-group">
    <% @users.each do |user| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= user.name %>
        <div>
        <span class="badge bg-primary rounded-pill">ID: <%= user.id %></span>
        <a class="link-offset-2 link-underline link-underline-opacity-0" href="<%= "/users/#{user.id}" %>">👀</a>
        </div>
      </li>
    <% end %>
  </ul>
</div>

<p class="mt-3 text-muted">Find me in <code>app/views/users/index.html.erb</code></p>
